// Code generated by Fern. DO NOT EDIT.

package roamhq

import (
	json "encoding/json"
	fmt "fmt"
	big "math/big"
	internal "ro.am/roamhq/internal"
	time "time"
)

var (
	chatItemFieldID        = big.NewInt(1 << 0)
	chatItemFieldType      = big.NewInt(1 << 1)
	chatItemFieldMime      = big.NewInt(1 << 2)
	chatItemFieldCreated   = big.NewInt(1 << 3)
	chatItemFieldName      = big.NewInt(1 << 4)
	chatItemFieldURL       = big.NewInt(1 << 5)
	chatItemFieldThumbnail = big.NewInt(1 << 6)
	chatItemFieldSize      = big.NewInt(1 << 7)
	chatItemFieldWidth     = big.NewInt(1 << 8)
	chatItemFieldHeight    = big.NewInt(1 << 9)
)

type ChatItem struct {
	// UUID identifying this item
	ID string `json:"id" url:"id"`
	// Type of item:
	//
	// - **photo**: Images with inline preview and thumbnail
	//   - image/jpeg, image/png, image/gif, image/webp
	//
	// - **blob**: Any other file type (download only, no preview)
	//   - application/octet-stream
	Type ChatItemType `json:"type" url:"type"`
	// MIME type of the file (e.g., "application/octet-stream").
	// May be omitted for photo items where the type is inferred from the image format.
	Mime *string `json:"mime,omitempty" url:"mime,omitempty"`
	// Timestamp when the item was created
	Created time.Time `json:"created" url:"created"`
	// Name of the item (typically the filename).
	Name string `json:"name" url:"name"`
	// URL for the uploaded item.
	URL string `json:"url" url:"url"`
	// URL for a thumbnail of the uploaded item (photo type only).
	// This may be equal to the item's main URL if it is suitable to use as a thumbnail.
	Thumbnail *string `json:"thumbnail,omitempty" url:"thumbnail,omitempty"`
	// Size of the item in bytes
	Size *int `json:"size,omitempty" url:"size,omitempty"`
	// Width in pixels (images only)
	Width *int `json:"width,omitempty" url:"width,omitempty"`
	// Height in pixels (images only)
	Height *int `json:"height,omitempty" url:"height,omitempty"`

	// Private bitmask of fields set to an explicit value and therefore not to be omitted
	explicitFields *big.Int `json:"-" url:"-"`

	extraProperties map[string]interface{}
	rawJSON         json.RawMessage
}

func (c *ChatItem) GetID() string {
	if c == nil {
		return ""
	}
	return c.ID
}

func (c *ChatItem) GetType() ChatItemType {
	if c == nil {
		return ""
	}
	return c.Type
}

func (c *ChatItem) GetMime() *string {
	if c == nil {
		return nil
	}
	return c.Mime
}

func (c *ChatItem) GetCreated() time.Time {
	if c == nil {
		return time.Time{}
	}
	return c.Created
}

func (c *ChatItem) GetName() string {
	if c == nil {
		return ""
	}
	return c.Name
}

func (c *ChatItem) GetURL() string {
	if c == nil {
		return ""
	}
	return c.URL
}

func (c *ChatItem) GetThumbnail() *string {
	if c == nil {
		return nil
	}
	return c.Thumbnail
}

func (c *ChatItem) GetSize() *int {
	if c == nil {
		return nil
	}
	return c.Size
}

func (c *ChatItem) GetWidth() *int {
	if c == nil {
		return nil
	}
	return c.Width
}

func (c *ChatItem) GetHeight() *int {
	if c == nil {
		return nil
	}
	return c.Height
}

func (c *ChatItem) GetExtraProperties() map[string]interface{} {
	if c == nil {
		return nil
	}
	return c.extraProperties
}

func (c *ChatItem) require(field *big.Int) {
	if c.explicitFields == nil {
		c.explicitFields = big.NewInt(0)
	}
	c.explicitFields.Or(c.explicitFields, field)
}

// SetID sets the ID field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (c *ChatItem) SetID(id string) {
	c.ID = id
	c.require(chatItemFieldID)
}

// SetType sets the Type field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (c *ChatItem) SetType(type_ ChatItemType) {
	c.Type = type_
	c.require(chatItemFieldType)
}

// SetMime sets the Mime field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (c *ChatItem) SetMime(mime *string) {
	c.Mime = mime
	c.require(chatItemFieldMime)
}

// SetCreated sets the Created field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (c *ChatItem) SetCreated(created time.Time) {
	c.Created = created
	c.require(chatItemFieldCreated)
}

// SetName sets the Name field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (c *ChatItem) SetName(name string) {
	c.Name = name
	c.require(chatItemFieldName)
}

// SetURL sets the URL field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (c *ChatItem) SetURL(url string) {
	c.URL = url
	c.require(chatItemFieldURL)
}

// SetThumbnail sets the Thumbnail field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (c *ChatItem) SetThumbnail(thumbnail *string) {
	c.Thumbnail = thumbnail
	c.require(chatItemFieldThumbnail)
}

// SetSize sets the Size field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (c *ChatItem) SetSize(size *int) {
	c.Size = size
	c.require(chatItemFieldSize)
}

// SetWidth sets the Width field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (c *ChatItem) SetWidth(width *int) {
	c.Width = width
	c.require(chatItemFieldWidth)
}

// SetHeight sets the Height field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (c *ChatItem) SetHeight(height *int) {
	c.Height = height
	c.require(chatItemFieldHeight)
}

func (c *ChatItem) UnmarshalJSON(data []byte) error {
	type embed ChatItem
	var unmarshaler = struct {
		embed
		Created *internal.DateTime `json:"created"`
	}{
		embed: embed(*c),
	}
	if err := json.Unmarshal(data, &unmarshaler); err != nil {
		return err
	}
	*c = ChatItem(unmarshaler.embed)
	c.Created = unmarshaler.Created.Time()
	extraProperties, err := internal.ExtractExtraProperties(data, *c)
	if err != nil {
		return err
	}
	c.extraProperties = extraProperties
	c.rawJSON = json.RawMessage(data)
	return nil
}

func (c *ChatItem) MarshalJSON() ([]byte, error) {
	type embed ChatItem
	var marshaler = struct {
		embed
		Created *internal.DateTime `json:"created"`
	}{
		embed:   embed(*c),
		Created: internal.NewDateTime(c.Created),
	}
	explicitMarshaler := internal.HandleExplicitFields(marshaler, c.explicitFields)
	return json.Marshal(explicitMarshaler)
}

func (c *ChatItem) String() string {
	if c == nil {
		return "<nil>"
	}
	if len(c.rawJSON) > 0 {
		if value, err := internal.StringifyJSON(c.rawJSON); err == nil {
			return value
		}
	}
	if value, err := internal.StringifyJSON(c); err == nil {
		return value
	}
	return fmt.Sprintf("%#v", c)
}

// Type of item:
//
// - **photo**: Images with inline preview and thumbnail
//   - image/jpeg, image/png, image/gif, image/webp
//
// - **blob**: Any other file type (download only, no preview)
//   - application/octet-stream
type ChatItemType string

const (
	ChatItemTypePhoto ChatItemType = "photo"
	ChatItemTypeBlob  ChatItemType = "blob"
)

func NewChatItemTypeFromString(s string) (ChatItemType, error) {
	switch s {
	case "photo":
		return ChatItemTypePhoto, nil
	case "blob":
		return ChatItemTypeBlob, nil
	}
	var t ChatItemType
	return "", fmt.Errorf("%s is not a valid %T", s, t)
}

func (c ChatItemType) Ptr() *ChatItemType {
	return &c
}

var (
	errorFieldOk       = big.NewInt(1 << 0)
	errorFieldError    = big.NewInt(1 << 1)
	errorFieldNeeded   = big.NewInt(1 << 2)
	errorFieldProvided = big.NewInt(1 << 3)
)

type Error struct {
	// Always `false` on error responses.
	Ok bool `json:"ok" url:"ok"`
	// Machine-readable error code from the catalog
	// (e.g. `invalid_token`, `missing_scope`, `ratelimited`, `invalid_cursor`).
	// Branch on this field. See [Responses and Errors](https://developer.ro.am/docs/guides/responses-and-errors).
	Error string `json:"error" url:"error"`
	// Present on `missing_scope`. Scopes that would satisfy the check.
	// **Any-of (OR)** semantics: holding any one element is enough.
	// Distinct from Slack's comma-separated `needed` string.
	Needed []string `json:"needed,omitempty" url:"needed,omitempty"`
	// Present on `missing_scope`. The token's granted scopes after alias
	// normalization (e.g. legacy `groups:read` reports as `group:read`).
	// For personal tokens this is the expanded OAuth set, not `pat:*`
	// group names.
	Provided []string `json:"provided,omitempty" url:"provided,omitempty"`

	// Private bitmask of fields set to an explicit value and therefore not to be omitted
	explicitFields *big.Int `json:"-" url:"-"`

	extraProperties map[string]interface{}
	rawJSON         json.RawMessage
}

func (e *Error) GetOk() bool {
	if e == nil {
		return false
	}
	return e.Ok
}

func (e *Error) GetError() string {
	if e == nil {
		return ""
	}
	return e.Error
}

func (e *Error) GetNeeded() []string {
	if e == nil {
		return nil
	}
	return e.Needed
}

func (e *Error) GetProvided() []string {
	if e == nil {
		return nil
	}
	return e.Provided
}

func (e *Error) GetExtraProperties() map[string]interface{} {
	if e == nil {
		return nil
	}
	return e.extraProperties
}

func (e *Error) require(field *big.Int) {
	if e.explicitFields == nil {
		e.explicitFields = big.NewInt(0)
	}
	e.explicitFields.Or(e.explicitFields, field)
}

// SetOk sets the Ok field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (e *Error) SetOk(ok bool) {
	e.Ok = ok
	e.require(errorFieldOk)
}

// SetError sets the Error field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (e *Error) SetError(error_ string) {
	e.Error = error_
	e.require(errorFieldError)
}

// SetNeeded sets the Needed field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (e *Error) SetNeeded(needed []string) {
	e.Needed = needed
	e.require(errorFieldNeeded)
}

// SetProvided sets the Provided field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (e *Error) SetProvided(provided []string) {
	e.Provided = provided
	e.require(errorFieldProvided)
}

func (e *Error) UnmarshalJSON(data []byte) error {
	type unmarshaler Error
	var value unmarshaler
	if err := json.Unmarshal(data, &value); err != nil {
		return err
	}
	*e = Error(value)
	extraProperties, err := internal.ExtractExtraProperties(data, *e)
	if err != nil {
		return err
	}
	e.extraProperties = extraProperties
	e.rawJSON = json.RawMessage(data)
	return nil
}

func (e *Error) MarshalJSON() ([]byte, error) {
	type embed Error
	var marshaler = struct {
		embed
	}{
		embed: embed(*e),
	}
	explicitMarshaler := internal.HandleExplicitFields(marshaler, e.explicitFields)
	return json.Marshal(explicitMarshaler)
}

func (e *Error) String() string {
	if e == nil {
		return "<nil>"
	}
	if len(e.rawJSON) > 0 {
		if value, err := internal.StringifyJSON(e.rawJSON); err == nil {
			return value
		}
	}
	if value, err := internal.StringifyJSON(e); err == nil {
		return value
	}
	return fmt.Sprintf("%#v", e)
}

// Out-of-office / "Will Return" status. A user can be `checkedIn` and still
// have `willReturn` (multi-day Out of Roam) — key off the presence of this
// object rather than `status` alone.
//
// On `user.info` / `user.list` (with `expand=status`) and `user.status.update`
// webhooks, elapsed return times are omitted. On `user.status.set` the written
// value is always returned.
var (
	willReturnFieldReturnTime = big.NewInt(1 << 0)
	willReturnFieldReason     = big.NewInt(1 << 1)
	willReturnFieldOutOfRoam  = big.NewInt(1 << 2)
)

type WillReturn struct {
	// When the user is expected to return (RFC 3339).
	ReturnTime time.Time `json:"returnTime" url:"returnTime"`
	// Optional absence message (for example "On Vacation" or "Out to lunch").
	// At most 128 Unicode code points.
	Reason *string `json:"reason,omitempty" url:"reason,omitempty"`
	// When true, multi-day Out of Roam that persists across check-ins. When
	// false or omitted on a **read**, same-day Will Return Today.
	//
	// `user.status.set` defaults omitted `outOfRoam` to **true**. Will Return
	// Today must send `outOfRoam: false` explicitly, and `returnTime` must be
	// less than 10 hours from now.
	OutOfRoam *bool `json:"outOfRoam,omitempty" url:"outOfRoam,omitempty"`

	// Private bitmask of fields set to an explicit value and therefore not to be omitted
	explicitFields *big.Int `json:"-" url:"-"`

	extraProperties map[string]interface{}
	rawJSON         json.RawMessage
}

func (w *WillReturn) GetReturnTime() time.Time {
	if w == nil {
		return time.Time{}
	}
	return w.ReturnTime
}

func (w *WillReturn) GetReason() *string {
	if w == nil {
		return nil
	}
	return w.Reason
}

func (w *WillReturn) GetOutOfRoam() *bool {
	if w == nil {
		return nil
	}
	return w.OutOfRoam
}

func (w *WillReturn) GetExtraProperties() map[string]interface{} {
	if w == nil {
		return nil
	}
	return w.extraProperties
}

func (w *WillReturn) require(field *big.Int) {
	if w.explicitFields == nil {
		w.explicitFields = big.NewInt(0)
	}
	w.explicitFields.Or(w.explicitFields, field)
}

// SetReturnTime sets the ReturnTime field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (w *WillReturn) SetReturnTime(returnTime time.Time) {
	w.ReturnTime = returnTime
	w.require(willReturnFieldReturnTime)
}

// SetReason sets the Reason field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (w *WillReturn) SetReason(reason *string) {
	w.Reason = reason
	w.require(willReturnFieldReason)
}

// SetOutOfRoam sets the OutOfRoam field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (w *WillReturn) SetOutOfRoam(outOfRoam *bool) {
	w.OutOfRoam = outOfRoam
	w.require(willReturnFieldOutOfRoam)
}

func (w *WillReturn) UnmarshalJSON(data []byte) error {
	type embed WillReturn
	var unmarshaler = struct {
		embed
		ReturnTime *internal.DateTime `json:"returnTime"`
	}{
		embed: embed(*w),
	}
	if err := json.Unmarshal(data, &unmarshaler); err != nil {
		return err
	}
	*w = WillReturn(unmarshaler.embed)
	w.ReturnTime = unmarshaler.ReturnTime.Time()
	extraProperties, err := internal.ExtractExtraProperties(data, *w)
	if err != nil {
		return err
	}
	w.extraProperties = extraProperties
	w.rawJSON = json.RawMessage(data)
	return nil
}

func (w *WillReturn) MarshalJSON() ([]byte, error) {
	type embed WillReturn
	var marshaler = struct {
		embed
		ReturnTime *internal.DateTime `json:"returnTime"`
	}{
		embed:      embed(*w),
		ReturnTime: internal.NewDateTime(w.ReturnTime),
	}
	explicitMarshaler := internal.HandleExplicitFields(marshaler, w.explicitFields)
	return json.Marshal(explicitMarshaler)
}

func (w *WillReturn) String() string {
	if w == nil {
		return "<nil>"
	}
	if len(w.rawJSON) > 0 {
		if value, err := internal.StringifyJSON(w.rawJSON); err == nil {
			return value
		}
	}
	if value, err := internal.StringifyJSON(w); err == nil {
		return value
	}
	return fmt.Sprintf("%#v", w)
}
