// Code generated by Fern. DO NOT EDIT.

package roamhq

import (
	json "encoding/json"
	fmt "fmt"
	big "math/big"
	internal "ro.am/roamhq/internal"
)

var (
	addReactionRequestFieldChatID          = big.NewInt(1 << 0)
	addReactionRequestFieldTimestamp       = big.NewInt(1 << 1)
	addReactionRequestFieldThreadTimestamp = big.NewInt(1 << 2)
	addReactionRequestFieldName            = big.NewInt(1 << 3)
)

type AddReactionRequest struct {
	// The chat containing the message to react to.
	ChatID string `json:"chatId" url:"-"`
	// Timestamp of the message to react to (Unix microseconds).
	Timestamp int `json:"timestamp" url:"-"`
	// Timestamp of the parent thread message (Unix microseconds), if reacting to a thread reply.
	ThreadTimestamp *int `json:"threadTimestamp,omitempty" url:"-"`
	// Name of the reaction to add (e.g. "thumbs_up", "heart", "100").
	Name string `json:"name" url:"-"`

	// Private bitmask of fields set to an explicit value and therefore not to be omitted
	explicitFields *big.Int `json:"-" url:"-"`
}

func (a *AddReactionRequest) require(field *big.Int) {
	if a.explicitFields == nil {
		a.explicitFields = big.NewInt(0)
	}
	a.explicitFields.Or(a.explicitFields, field)
}

// SetChatID sets the ChatID field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (a *AddReactionRequest) SetChatID(chatID string) {
	a.ChatID = chatID
	a.require(addReactionRequestFieldChatID)
}

// SetTimestamp sets the Timestamp field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (a *AddReactionRequest) SetTimestamp(timestamp int) {
	a.Timestamp = timestamp
	a.require(addReactionRequestFieldTimestamp)
}

// SetThreadTimestamp sets the ThreadTimestamp field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (a *AddReactionRequest) SetThreadTimestamp(threadTimestamp *int) {
	a.ThreadTimestamp = threadTimestamp
	a.require(addReactionRequestFieldThreadTimestamp)
}

// SetName sets the Name field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (a *AddReactionRequest) SetName(name string) {
	a.Name = name
	a.require(addReactionRequestFieldName)
}

func (a *AddReactionRequest) UnmarshalJSON(data []byte) error {
	type unmarshaler AddReactionRequest
	var body unmarshaler
	if err := json.Unmarshal(data, &body); err != nil {
		return err
	}
	*a = AddReactionRequest(body)
	return nil
}

func (a *AddReactionRequest) MarshalJSON() ([]byte, error) {
	type embed AddReactionRequest
	var marshaler = struct {
		embed
	}{
		embed: embed(*a),
	}
	explicitMarshaler := internal.HandleExplicitFields(marshaler, a.explicitFields)
	return json.Marshal(explicitMarshaler)
}

var (
	listReactionRequestFieldChatID          = big.NewInt(1 << 0)
	listReactionRequestFieldTimestamp       = big.NewInt(1 << 1)
	listReactionRequestFieldThreadTimestamp = big.NewInt(1 << 2)
)

type ListReactionRequest struct {
	// The chat containing the message.
	ChatID string `json:"-" url:"chatId"`
	// Timestamp of the message (Unix microseconds).
	Timestamp int `json:"-" url:"timestamp"`
	// Timestamp of the parent thread message (Unix microseconds), if listing reactions on a thread reply.
	ThreadTimestamp *int `json:"-" url:"threadTimestamp,omitempty"`

	// Private bitmask of fields set to an explicit value and therefore not to be omitted
	explicitFields *big.Int `json:"-" url:"-"`
}

func (l *ListReactionRequest) require(field *big.Int) {
	if l.explicitFields == nil {
		l.explicitFields = big.NewInt(0)
	}
	l.explicitFields.Or(l.explicitFields, field)
}

// SetChatID sets the ChatID field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListReactionRequest) SetChatID(chatID string) {
	l.ChatID = chatID
	l.require(listReactionRequestFieldChatID)
}

// SetTimestamp sets the Timestamp field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListReactionRequest) SetTimestamp(timestamp int) {
	l.Timestamp = timestamp
	l.require(listReactionRequestFieldTimestamp)
}

// SetThreadTimestamp sets the ThreadTimestamp field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListReactionRequest) SetThreadTimestamp(threadTimestamp *int) {
	l.ThreadTimestamp = threadTimestamp
	l.require(listReactionRequestFieldThreadTimestamp)
}

var (
	removeReactionRequestFieldChatID          = big.NewInt(1 << 0)
	removeReactionRequestFieldTimestamp       = big.NewInt(1 << 1)
	removeReactionRequestFieldThreadTimestamp = big.NewInt(1 << 2)
	removeReactionRequestFieldName            = big.NewInt(1 << 3)
)

type RemoveReactionRequest struct {
	// The chat containing the message.
	ChatID string `json:"chatId" url:"-"`
	// Timestamp of the message (Unix microseconds).
	Timestamp int `json:"timestamp" url:"-"`
	// Timestamp of the parent thread message (Unix microseconds), if removing from a thread reply.
	ThreadTimestamp *int `json:"threadTimestamp,omitempty" url:"-"`
	// Name of the reaction to remove (e.g. "thumbs_up", "heart").
	Name string `json:"name" url:"-"`

	// Private bitmask of fields set to an explicit value and therefore not to be omitted
	explicitFields *big.Int `json:"-" url:"-"`
}

func (r *RemoveReactionRequest) require(field *big.Int) {
	if r.explicitFields == nil {
		r.explicitFields = big.NewInt(0)
	}
	r.explicitFields.Or(r.explicitFields, field)
}

// SetChatID sets the ChatID field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (r *RemoveReactionRequest) SetChatID(chatID string) {
	r.ChatID = chatID
	r.require(removeReactionRequestFieldChatID)
}

// SetTimestamp sets the Timestamp field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (r *RemoveReactionRequest) SetTimestamp(timestamp int) {
	r.Timestamp = timestamp
	r.require(removeReactionRequestFieldTimestamp)
}

// SetThreadTimestamp sets the ThreadTimestamp field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (r *RemoveReactionRequest) SetThreadTimestamp(threadTimestamp *int) {
	r.ThreadTimestamp = threadTimestamp
	r.require(removeReactionRequestFieldThreadTimestamp)
}

// SetName sets the Name field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (r *RemoveReactionRequest) SetName(name string) {
	r.Name = name
	r.require(removeReactionRequestFieldName)
}

func (r *RemoveReactionRequest) UnmarshalJSON(data []byte) error {
	type unmarshaler RemoveReactionRequest
	var body unmarshaler
	if err := json.Unmarshal(data, &body); err != nil {
		return err
	}
	*r = RemoveReactionRequest(body)
	return nil
}

func (r *RemoveReactionRequest) MarshalJSON() ([]byte, error) {
	type embed RemoveReactionRequest
	var marshaler = struct {
		embed
	}{
		embed: embed(*r),
	}
	explicitMarshaler := internal.HandleExplicitFields(marshaler, r.explicitFields)
	return json.Marshal(explicitMarshaler)
}

// One emoji reaction on a message, grouped across reactors
// (Slack-style `{name, count, users}`).
var (
	reactionFieldName  = big.NewInt(1 << 0)
	reactionFieldCount = big.NewInt(1 << 1)
	reactionFieldUsers = big.NewInt(1 << 2)
)

type Reaction struct {
	// Reaction shortcode without surrounding colons (e.g. `thumbs_up`, `wave`,
	// `heart`). Matches the `name` accepted by `reaction.add` / `reaction.remove`
	// and delivered on the `chat.reaction` webhook.
	Name string `json:"name" url:"name"`
	// Number of users who added this reaction.
	Count int `json:"count" url:"count"`
	// Visible principal IDs of reactors. Hydrate with `user.list?ids`; unauthorized IDs are omitted and `count` reflects this array.
	Users []string `json:"users" url:"users"`

	// Private bitmask of fields set to an explicit value and therefore not to be omitted
	explicitFields *big.Int `json:"-" url:"-"`

	extraProperties map[string]interface{}
	rawJSON         json.RawMessage
}

func (r *Reaction) GetName() string {
	if r == nil {
		return ""
	}
	return r.Name
}

func (r *Reaction) GetCount() int {
	if r == nil {
		return 0
	}
	return r.Count
}

func (r *Reaction) GetUsers() []string {
	if r == nil {
		return nil
	}
	return r.Users
}

func (r *Reaction) GetExtraProperties() map[string]interface{} {
	if r == nil {
		return nil
	}
	return r.extraProperties
}

func (r *Reaction) require(field *big.Int) {
	if r.explicitFields == nil {
		r.explicitFields = big.NewInt(0)
	}
	r.explicitFields.Or(r.explicitFields, field)
}

// SetName sets the Name field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (r *Reaction) SetName(name string) {
	r.Name = name
	r.require(reactionFieldName)
}

// SetCount sets the Count field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (r *Reaction) SetCount(count int) {
	r.Count = count
	r.require(reactionFieldCount)
}

// SetUsers sets the Users field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (r *Reaction) SetUsers(users []string) {
	r.Users = users
	r.require(reactionFieldUsers)
}

func (r *Reaction) UnmarshalJSON(data []byte) error {
	type unmarshaler Reaction
	var value unmarshaler
	if err := json.Unmarshal(data, &value); err != nil {
		return err
	}
	*r = Reaction(value)
	extraProperties, err := internal.ExtractExtraProperties(data, *r)
	if err != nil {
		return err
	}
	r.extraProperties = extraProperties
	r.rawJSON = json.RawMessage(data)
	return nil
}

func (r *Reaction) MarshalJSON() ([]byte, error) {
	type embed Reaction
	var marshaler = struct {
		embed
	}{
		embed: embed(*r),
	}
	explicitMarshaler := internal.HandleExplicitFields(marshaler, r.explicitFields)
	return json.Marshal(explicitMarshaler)
}

func (r *Reaction) String() string {
	if r == nil {
		return "<nil>"
	}
	if len(r.rawJSON) > 0 {
		if value, err := internal.StringifyJSON(r.rawJSON); err == nil {
			return value
		}
	}
	if value, err := internal.StringifyJSON(r); err == nil {
		return value
	}
	return fmt.Sprintf("%#v", r)
}

var (
	listReactionResponseFieldOk              = big.NewInt(1 << 0)
	listReactionResponseFieldChatID          = big.NewInt(1 << 1)
	listReactionResponseFieldTimestamp       = big.NewInt(1 << 2)
	listReactionResponseFieldThreadTimestamp = big.NewInt(1 << 3)
	listReactionResponseFieldReactions       = big.NewInt(1 << 4)
	listReactionResponseFieldPollVotes       = big.NewInt(1 << 5)
)

type ListReactionResponse struct {
	Ok *bool `json:"ok,omitempty" url:"ok,omitempty"`
	// The chat containing the message.
	ChatID string `json:"chatId" url:"chatId"`
	// Timestamp of the message (Unix microseconds).
	Timestamp int `json:"timestamp" url:"timestamp"`
	// Parent thread timestamp when the message is a thread reply.
	ThreadTimestamp *int `json:"threadTimestamp,omitempty" url:"threadTimestamp,omitempty"`
	// Emoji reactions, one entry per distinct reaction name.
	Reactions []*Reaction `json:"reactions" url:"reactions"`
	// Poll option votes when the message is a poll. Not included among
	// `reactions`.
	PollVotes []*ListReactionResponsePollVotesItem `json:"pollVotes" url:"pollVotes"`

	// Private bitmask of fields set to an explicit value and therefore not to be omitted
	explicitFields *big.Int `json:"-" url:"-"`

	extraProperties map[string]interface{}
	rawJSON         json.RawMessage
}

func (l *ListReactionResponse) GetOk() *bool {
	if l == nil {
		return nil
	}
	return l.Ok
}

func (l *ListReactionResponse) GetChatID() string {
	if l == nil {
		return ""
	}
	return l.ChatID
}

func (l *ListReactionResponse) GetTimestamp() int {
	if l == nil {
		return 0
	}
	return l.Timestamp
}

func (l *ListReactionResponse) GetThreadTimestamp() *int {
	if l == nil {
		return nil
	}
	return l.ThreadTimestamp
}

func (l *ListReactionResponse) GetReactions() []*Reaction {
	if l == nil {
		return nil
	}
	return l.Reactions
}

func (l *ListReactionResponse) GetPollVotes() []*ListReactionResponsePollVotesItem {
	if l == nil {
		return nil
	}
	return l.PollVotes
}

func (l *ListReactionResponse) GetExtraProperties() map[string]interface{} {
	if l == nil {
		return nil
	}
	return l.extraProperties
}

func (l *ListReactionResponse) require(field *big.Int) {
	if l.explicitFields == nil {
		l.explicitFields = big.NewInt(0)
	}
	l.explicitFields.Or(l.explicitFields, field)
}

// SetOk sets the Ok field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListReactionResponse) SetOk(ok *bool) {
	l.Ok = ok
	l.require(listReactionResponseFieldOk)
}

// SetChatID sets the ChatID field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListReactionResponse) SetChatID(chatID string) {
	l.ChatID = chatID
	l.require(listReactionResponseFieldChatID)
}

// SetTimestamp sets the Timestamp field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListReactionResponse) SetTimestamp(timestamp int) {
	l.Timestamp = timestamp
	l.require(listReactionResponseFieldTimestamp)
}

// SetThreadTimestamp sets the ThreadTimestamp field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListReactionResponse) SetThreadTimestamp(threadTimestamp *int) {
	l.ThreadTimestamp = threadTimestamp
	l.require(listReactionResponseFieldThreadTimestamp)
}

// SetReactions sets the Reactions field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListReactionResponse) SetReactions(reactions []*Reaction) {
	l.Reactions = reactions
	l.require(listReactionResponseFieldReactions)
}

// SetPollVotes sets the PollVotes field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListReactionResponse) SetPollVotes(pollVotes []*ListReactionResponsePollVotesItem) {
	l.PollVotes = pollVotes
	l.require(listReactionResponseFieldPollVotes)
}

func (l *ListReactionResponse) UnmarshalJSON(data []byte) error {
	type unmarshaler ListReactionResponse
	var value unmarshaler
	if err := json.Unmarshal(data, &value); err != nil {
		return err
	}
	*l = ListReactionResponse(value)
	extraProperties, err := internal.ExtractExtraProperties(data, *l)
	if err != nil {
		return err
	}
	l.extraProperties = extraProperties
	l.rawJSON = json.RawMessage(data)
	return nil
}

func (l *ListReactionResponse) MarshalJSON() ([]byte, error) {
	type embed ListReactionResponse
	var marshaler = struct {
		embed
	}{
		embed: embed(*l),
	}
	explicitMarshaler := internal.HandleExplicitFields(marshaler, l.explicitFields)
	return json.Marshal(explicitMarshaler)
}

func (l *ListReactionResponse) String() string {
	if l == nil {
		return "<nil>"
	}
	if len(l.rawJSON) > 0 {
		if value, err := internal.StringifyJSON(l.rawJSON); err == nil {
			return value
		}
	}
	if value, err := internal.StringifyJSON(l); err == nil {
		return value
	}
	return fmt.Sprintf("%#v", l)
}

var (
	listReactionResponsePollVotesItemFieldOptionID = big.NewInt(1 << 0)
	listReactionResponsePollVotesItemFieldText     = big.NewInt(1 << 1)
	listReactionResponsePollVotesItemFieldCount    = big.NewInt(1 << 2)
	listReactionResponsePollVotesItemFieldUsers    = big.NewInt(1 << 3)
)

type ListReactionResponsePollVotesItem struct {
	// Poll option identifier.
	OptionID string `json:"optionId" url:"optionId"`
	// Option display text (omitted if the option no longer resolves).
	Text  *string  `json:"text,omitempty" url:"text,omitempty"`
	Count int      `json:"count" url:"count"`
	Users []string `json:"users" url:"users"`

	// Private bitmask of fields set to an explicit value and therefore not to be omitted
	explicitFields *big.Int `json:"-" url:"-"`

	extraProperties map[string]interface{}
	rawJSON         json.RawMessage
}

func (l *ListReactionResponsePollVotesItem) GetOptionID() string {
	if l == nil {
		return ""
	}
	return l.OptionID
}

func (l *ListReactionResponsePollVotesItem) GetText() *string {
	if l == nil {
		return nil
	}
	return l.Text
}

func (l *ListReactionResponsePollVotesItem) GetCount() int {
	if l == nil {
		return 0
	}
	return l.Count
}

func (l *ListReactionResponsePollVotesItem) GetUsers() []string {
	if l == nil {
		return nil
	}
	return l.Users
}

func (l *ListReactionResponsePollVotesItem) GetExtraProperties() map[string]interface{} {
	if l == nil {
		return nil
	}
	return l.extraProperties
}

func (l *ListReactionResponsePollVotesItem) require(field *big.Int) {
	if l.explicitFields == nil {
		l.explicitFields = big.NewInt(0)
	}
	l.explicitFields.Or(l.explicitFields, field)
}

// SetOptionID sets the OptionID field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListReactionResponsePollVotesItem) SetOptionID(optionID string) {
	l.OptionID = optionID
	l.require(listReactionResponsePollVotesItemFieldOptionID)
}

// SetText sets the Text field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListReactionResponsePollVotesItem) SetText(text *string) {
	l.Text = text
	l.require(listReactionResponsePollVotesItemFieldText)
}

// SetCount sets the Count field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListReactionResponsePollVotesItem) SetCount(count int) {
	l.Count = count
	l.require(listReactionResponsePollVotesItemFieldCount)
}

// SetUsers sets the Users field and marks it as non-optional;
// this prevents an empty or null value for this field from being omitted during serialization.
func (l *ListReactionResponsePollVotesItem) SetUsers(users []string) {
	l.Users = users
	l.require(listReactionResponsePollVotesItemFieldUsers)
}

func (l *ListReactionResponsePollVotesItem) UnmarshalJSON(data []byte) error {
	type unmarshaler ListReactionResponsePollVotesItem
	var value unmarshaler
	if err := json.Unmarshal(data, &value); err != nil {
		return err
	}
	*l = ListReactionResponsePollVotesItem(value)
	extraProperties, err := internal.ExtractExtraProperties(data, *l)
	if err != nil {
		return err
	}
	l.extraProperties = extraProperties
	l.rawJSON = json.RawMessage(data)
	return nil
}

func (l *ListReactionResponsePollVotesItem) MarshalJSON() ([]byte, error) {
	type embed ListReactionResponsePollVotesItem
	var marshaler = struct {
		embed
	}{
		embed: embed(*l),
	}
	explicitMarshaler := internal.HandleExplicitFields(marshaler, l.explicitFields)
	return json.Marshal(explicitMarshaler)
}

func (l *ListReactionResponsePollVotesItem) String() string {
	if l == nil {
		return "<nil>"
	}
	if len(l.rawJSON) > 0 {
		if value, err := internal.StringifyJSON(l.rawJSON); err == nil {
			return value
		}
	}
	if value, err := internal.StringifyJSON(l); err == nil {
		return value
	}
	return fmt.Sprintf("%#v", l)
}
